# Production-like compose entrypoint. For local demo auth, use docker-compose.demo.yml.
services:
  migrate:
    extends:
      file: docker-compose.prod.yml
      service: migrate

  app:
    extends:
      file: docker-compose.prod.yml
      service: app

networks:
  legacy-lens-network:
    driver: bridge
