package analyzer

import (
	"fmt"
	"sort"
	"strings"
)

// DocumentGenerator 文件生成器
type DocumentGenerator struct {
	parseResult *ParseResult
	risks       []*RiskItem
	projectName string
}

// NewDocumentGenerator 建立新的文件生成器
func NewDocumentGenerator(parseResult *ParseResult, risks []*RiskItem, projectName string) *DocumentGenerator {
	return &DocumentGenerator{
		parseResult: parseResult,
		risks:       risks,
		projectName: projectName,
	}
}

// GenerateFlowMarkdown 生成 FLOW.md
func (dg *DocumentGenerator) GenerateFlowMarkdown() string {
	var sb strings.Builder

	sb.WriteString("# 流程說明 (FLOW.md)\n\n")
	sb.WriteString(fmt.Sprintf("**專案名稱:** %s\n\n", dg.projectName))
	sb.WriteString("## 概述\n\n")
	sb.WriteString("本文件詳細說明了系統中各個主要功能的流程、入口點、執行步驟及例外處理。\n\n")

	// 按類型分組符號
	functionSymbols := make([]*Symbol, 0)
	procedureSymbols := make([]*Symbol, 0)
	querySymbols := make([]*Symbol, 0)

	for _, symbol := range dg.parseResult.Symbols {
		switch symbol.Type {\n		case \"function\", \"method\":\n			functionSymbols = append(functionSymbols, symbol)\n		case \"procedure\":\n			procedureSymbols = append(procedureSymbols, symbol)\n		case \"query\":\n			querySymbols = append(querySymbols, symbol)\n		}\n	}\n\n	// 生成函數流程\n	if len(functionSymbols) > 0 {\n		sb.WriteString(\"## 主要函數流程\\n\\n\")\n		for _, symbol := range functionSymbols {\n			sb.WriteString(fmt.Sprintf(\"### %s\\n\\n\", symbol.Name))\n			sb.WriteString(fmt.Sprintf(\"**簽名:** `%s`\\n\\n\", symbol.Signature))\n			sb.WriteString(fmt.Sprintf(\"**位置:** %s:%d-%d\\n\\n\", symbol.Location.File, symbol.Location.Line, symbol.Location.EndLine))\n			sb.WriteString(\"**執行步驟:**\\n\\n\")\n			sb.WriteString(\"1. 驗證輸入參數\\n\")\n			sb.WriteString(\"2. 執行核心邏輯\\n\")\n			sb.WriteString(\"3. 返回結果\\n\\n\")\n			sb.WriteString(\"**例外處理:**\\n\\n\")\n			sb.WriteString(\"- 參數驗證失敗時返回錯誤\\n\")\n			sb.WriteString(\"- 執行過程中的異常會被記錄並拋出\\n\\n\")\n		}\n	}\n\n	// 生成程序流程\n	if len(procedureSymbols) > 0 {\n		sb.WriteString(\"## 主要程序流程\\n\\n\")\n		for _, symbol := range procedureSymbols {\n			sb.WriteString(fmt.Sprintf(\"### %s\\n\\n\", symbol.Name))\n			sb.WriteString(fmt.Sprintf(\"**位置:** %s:%d-%d\\n\\n\", symbol.Location.File, symbol.Location.Line, symbol.Location.EndLine))\n			sb.WriteString(\"**執行流程:**\\n\\n\")\n			sb.WriteString(\"1. 初始化\\n\")\n			sb.WriteString(\"2. 處理業務邏輯\\n\")\n			sb.WriteString(\"3. 提交或回滾\\n\\n\")\n		}\n	}\n\n	// 生成查詢流程\n	if len(querySymbols) > 0 {\n		sb.WriteString(\"## 主要查詢\\n\\n\")\n		for i, symbol := range querySymbols {\n			if i > 10 { // 限制顯示數量\n				sb.WriteString(fmt.Sprintf(\"... 及其他 %d 個查詢\\n\\n\", len(querySymbols)-10))\n				break\n			}\n			sb.WriteString(fmt.Sprintf(\"### 查詢 %d\\n\\n\", i+1))\n			sb.WriteString(fmt.Sprintf(\"**位置:** %s:%d\\n\\n\", symbol.Location.File, symbol.Location.Line))\n			sb.WriteString(fmt.Sprintf(\"```sql\\n%s\\n```\\n\\n\", symbol.Content))\n		}\n	}\n\n	return sb.String()\n}\n\n// GenerateDataDependencyMarkdown 生成 DATA_DEPENDENCY.md\nfunc (dg *DocumentGenerator) GenerateDataDependencyMarkdown() string {\n	var sb strings.Builder\n\n	sb.WriteString(\"# 欄位依賴分析 (DATA_DEPENDENCY.md)\\n\\n\")\n	sb.WriteString(fmt.Sprintf(\"**專案名稱:** %s\\n\\n\", dg.projectName))\n	sb.WriteString(\"## 概述\\n\\n\")\n	sb.WriteString(\"本文件分析了系統中各個欄位的讀取、寫入和計算依賴關係。\\n\\n\")\n\n	// 按表名分組欄位引用\n	tableFieldMap := make(map[string]map[string][]*FieldReference)\n	for _, ref := range dg.parseResult.FieldReferences {\n		if tableFieldMap[ref.TableName] == nil {\n			tableFieldMap[ref.TableName] = make(map[string][]*FieldReference)\n		}\n		tableFieldMap[ref.TableName][ref.FieldName] = append(tableFieldMap[ref.TableName][ref.FieldName], ref)\n	}\n\n	// 排序表名\n	var tableNames []string\n	for tableName := range tableFieldMap {\n		tableNames = append(tableNames, tableName)\n	}\n	sort.Strings(tableNames)\n\n	for _, tableName := range tableNames {\n		sb.WriteString(fmt.Sprintf(\"## 表: %s\\n\\n\", tableName))\n\n		fieldMap := tableFieldMap[tableName]\n		var fieldNames []string\n		for fieldName := range fieldMap {\n			fieldNames = append(fieldNames, fieldName)\n		}\n		sort.Strings(fieldNames)\n\n		for _, fieldName := range fieldNames {\n			refs := fieldMap[fieldName]\n			sb.WriteString(fmt.Sprintf(\"### %s.%s\\n\\n\", tableName, fieldName))\n\n			// 分類操作類型\n			readOps := make([]*FieldReference, 0)\n			writeOps := make([]*FieldReference, 0)\n			calcOps := make([]*FieldReference, 0)\n\n			for _, ref := range refs {\n				switch ref.OperationType {\n				case \"read\":\n					readOps = append(readOps, ref)\n				case \"write\":\n					writeOps = append(writeOps, ref)\n				case \"calculate\":\n					calcOps = append(calcOps, ref)\n				}\n			}\n\n			// 顯示讀取操作\n			if len(readOps) > 0 {\n				sb.WriteString(\"**讀取 (READ):**\\n\\n\")\n				for _, ref := range readOps {\n					sb.WriteString(fmt.Sprintf(\"- %s:%d\\n\", ref.Location.File, ref.Location.Line))\n					sb.WriteString(fmt.Sprintf(\"  ```\\n  %s\\n  ```\\n\", ref.Context))\n				}\n				sb.WriteString(\"\\n\")\n			}\n\n			// 顯示寫入操作\n			if len(writeOps) > 0 {\n				sb.WriteString(\"**寫入 (WRITE):**\\n\\n\")\n				for _, ref := range writeOps {\n					sb.WriteString(fmt.Sprintf(\"- %s:%d\\n\", ref.Location.File, ref.Location.Line))\n					sb.WriteString(fmt.Sprintf(\"  ```\\n  %s\\n  ```\\n\", ref.Context))\n				}\n				sb.WriteString(\"\\n\")\n			}\n\n			// 顯示計算操作\n			if len(calcOps) > 0 {\n				sb.WriteString(\"**計算 (CALCULATE):**\\n\\n\")\n				for _, ref := range calcOps {\n					sb.WriteString(fmt.Sprintf(\"- %s:%d\\n\", ref.Location.File, ref.Location.Line))\n					sb.WriteString(fmt.Sprintf(\"  ```\\n  %s\\n  ```\\n\", ref.Context))\n				}\n				sb.WriteString(\"\\n\")\n			}\n		}\n	}\n\n	return sb.String()\n}\n\n// GenerateRisksMarkdown 生成 RISKS.md\nfunc (dg *DocumentGenerator) GenerateRisksMarkdown() string {\n	var sb strings.Builder\n\n	sb.WriteString(\"# 風險提示 (RISKS.md)\\n\\n\")\n	sb.WriteString(fmt.Sprintf(\"**專案名稱:** %s\\n\\n\", dg.projectName))\n	sb.WriteString(\"## 概述\\n\\n\")\n	sb.WriteString(fmt.Sprintf(\"本文件列出了系統中檢測到的 %d 個潛在風險項目。\\n\\n\", len(dg.risks)))\n\n	// 按嚴重程度分組\n	criticalRisks := make([]*RiskItem, 0)\n	highRisks := make([]*RiskItem, 0)\n	mediumRisks := make([]*RiskItem, 0)\n	lowRisks := make([]*RiskItem, 0)\n\n	for _, risk := range dg.risks {\n		switch risk.Severity {\n		case \"critical\":\n			criticalRisks = append(criticalRisks, risk)\n		case \"high\":\n			highRisks = append(highRisks, risk)\n		case \"medium\":\n			mediumRisks = append(mediumRisks, risk)\n		case \"low\":\n			lowRisks = append(lowRisks, risk)\n		}\n	}\n\n	// 顯示風險統計\n	sb.WriteString(\"## 風險統計\\n\\n\")\n	sb.WriteString(\"| 嚴重程度 | 數量 |\\n\")\n	sb.WriteString(\"|---------|------|\\n\")\n	sb.WriteString(fmt.Sprintf(\"| 🔴 Critical | %d |\\n\", len(criticalRisks)))\n	sb.WriteString(fmt.Sprintf(\"| 🟠 High | %d |\\n\", len(highRisks)))\n	sb.WriteString(fmt.Sprintf(\"| 🟡 Medium | %d |\\n\", len(mediumRisks)))\n	sb.WriteString(fmt.Sprintf(\"| 🟢 Low | %d |\\n\\n\", len(lowRisks)))\n\n	// 顯示 Critical 風險\n	if len(criticalRisks) > 0 {\n		sb.WriteString(\"## 🔴 Critical 風險\\n\\n\")\n		for i, risk := range criticalRisks {\n			sb.WriteString(fmt.Sprintf(\"### %d. %s\\n\\n\", i+1, risk.Title))\n			sb.WriteString(fmt.Sprintf(\"**類型:** %s\\n\\n\", risk.RiskType))\n			sb.WriteString(fmt.Sprintf(\"**描述:** %s\\n\\n\", risk.Description))\n			sb.WriteString(fmt.Sprintf(\"**位置:** %s:%d\\n\\n\", risk.SourceFile, risk.LineNumber))\n			sb.WriteString(fmt.Sprintf(\"**代碼片段:**\\n```\\n%s\\n```\\n\\n\", risk.CodeSnippet))\n			sb.WriteString(fmt.Sprintf(\"**建議:** %s\\n\\n\", risk.Recommendation))\n		}\n	}\n\n	// 顯示 High 風險\n	if len(highRisks) > 0 {\n		sb.WriteString(\"## 🟠 High 風險\\n\\n\")\n		for i, risk := range highRisks {\n			sb.WriteString(fmt.Sprintf(\"### %d. %s\\n\\n\", i+1, risk.Title))\n			sb.WriteString(fmt.Sprintf(\"**類型:** %s\\n\\n\", risk.RiskType))\n			sb.WriteString(fmt.Sprintf(\"**描述:** %s\\n\\n\", risk.Description))\n			sb.WriteString(fmt.Sprintf(\"**位置:** %s:%d\\n\\n\", risk.SourceFile, risk.LineNumber))\n			sb.WriteString(fmt.Sprintf(\"**建議:** %s\\n\\n\", risk.Recommendation))\n		}\n	}\n\n	// 顯示 Medium 風險\n	if len(mediumRisks) > 0 {\n		sb.WriteString(\"## 🟡 Medium 風險\\n\\n\")\n		for i, risk := range mediumRisks {\n			sb.WriteString(fmt.Sprintf(\"### %d. %s\\n\\n\", i+1, risk.Title))\n			sb.WriteString(fmt.Sprintf(\"**類型:** %s\\n\\n\", risk.RiskType))\n			sb.WriteString(fmt.Sprintf(\"**描述:** %s\\n\\n\", risk.Description))\n			sb.WriteString(fmt.Sprintf(\"**位置:** %s:%d\\n\\n\", risk.SourceFile, risk.LineNumber))\n		}\n	}\n\n	// 顯示 Low 風險\n	if len(lowRisks) > 0 {\n		sb.WriteString(\"## 🟢 Low 風險\\n\\n\")\n		for i, risk := range lowRisks {\n			sb.WriteString(fmt.Sprintf(\"### %d. %s\\n\\n\", i+1, risk.Title))\n			sb.WriteString(fmt.Sprintf(\"**類型:** %s\\n\\n\", risk.RiskType))\n			sb.WriteString(fmt.Sprintf(\"**描述:** %s\\n\\n\", risk.Description))\n		}\n	}\n\n	return sb.String()\n}\n\n// GenerateRulesYAML 生成 RULES.yaml\nfunc (dg *DocumentGenerator) GenerateRulesYAML() string {\n	var sb strings.Builder\n\n	sb.WriteString(\"# 規則定義 (RULES.yaml)\\n\\n\")\n	sb.WriteString(fmt.Sprintf(\"project_name: %s\\n\", dg.projectName))\n	sb.WriteString(\"rules:\\n\")\n\n	// 從風險項目中提取規則\n	ruleIndex := 1\n	for _, risk := range dg.risks {\n		if risk.RiskType == \"magic_value\" || risk.RiskType == \"format_conversion\" {\n			sb.WriteString(fmt.Sprintf(\"  - id: rule_%d\\n\", ruleIndex))\n			sb.WriteString(fmt.Sprintf(\"    name: %s\\n\", risk.Title))\n			sb.WriteString(fmt.Sprintf(\"    type: %s\\n\", risk.RiskType))\n			sb.WriteString(fmt.Sprintf(\"    severity: %s\\n\", risk.Severity))\n			sb.WriteString(fmt.Sprintf(\"    description: %s\\n\", risk.Description))\n			sb.WriteString(fmt.Sprintf(\"    source_file: %s\\n\", risk.SourceFile))\n			sb.WriteString(fmt.Sprintf(\"    line_number: %d\\n\", risk.LineNumber))\n			sb.WriteString(fmt.Sprintf(\"    recommendation: %s\\n\\n\", risk.Recommendation))\n			ruleIndex++\n		}\n	}\n\n	return sb.String()\n}\n
